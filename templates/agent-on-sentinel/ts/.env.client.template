# Environment configuration for the client
# Copy this file to .env.client and customize as needed.

# ─── Client Configuration ─────────────────────────────────────────────────────
# WebSocket URL for the client to connect to the sentinel
# Keep this URL in sync with FAME_LISTENER_PORT
FAME_DIRECT_ADMISSION_URL=ws://localhost:8000/fame/v1/attach/ws/downstream

# ─── Logging ──────────────────────────────────────────────────────────────────
# Log level: debug, info, warning, error
FAME_LOG_LEVEL=warning

# Show envelope traffic (useful for debugging)
FAME_SHOW_ENVELOPES=false
